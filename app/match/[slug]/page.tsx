export default function Match() {
  // Get match by id
  // Populate fields
  // If in progress - show timer + score buttons (2,3,4)
  // If if not started yet - show competitors
  // If over - show results
  return <h1>Match</h1>;
}
